package com.example.huda_application.user;

public enum AppointmentStatus {
    PENDING,
    APPROVED,                            // The appointment status enum used to store in the database of the application and for the checks for status.
    CANCELED,
    DENIED
}
